let monPC;
monPC = "Le PC de Thomas";
const monPCChaineDeCaractères = <string>monPC; // Forcer la conversion
let ram = 16; // Inférence de type (type inference)
const resultat: string = `${monPC} possède ${ram} go de ram !`;

export default {};
